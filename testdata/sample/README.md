# Sample data for small internet

Assume that whole internet consists of 3 sites:

in.com -> target.com -> out.com

`->` is HTTP link from one site to another


Command line to regenerate offsets

```bash
go run cmd/indexer/main.go testdata/sample
```
